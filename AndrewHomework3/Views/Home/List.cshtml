@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewBag.Title = "Movie List";
}

@model IEnumerable<AndrewHomework3.Models.MovieModel>

<br />
<table class="table table-striped table-hover">

    <tr>
        <th>Category</th>
        <th>Title</th>
        <th>Year</th>
        <th>Edited</th>
        <th>Lent To</th>
        <th>Notes</th>
        <th></th>
        <th></th>
    </tr>

    @foreach (MovieModel x in Model)
    {
        <tr>
            <td>@x.Category</td>
            <td>@x.Title</td>
            <td>@x.Year</td>
            <td>@x.Rating</td>
            <td>@x.LentTo</td>
            <td>@x.Notes</td>
            <td><a asp-action="Update" asp-route-movieid="@x.MovieID">Update</a></td>
            <td>
                <form asp-action="Delete" method="post">
                    <input type="hidden" name="movieid" value="@x.MovieID" />
                    <button type="submit">Delete</button>
                </form>
            </td>
        </tr>

    }

</table>
@*<div>
    <h4>Here are the individuals currently on the waitlist</h4><br />
    <table>
        <thead>
            <tr><th>Category</th><th>Title</th><th>Year</th><th>Rating</th><th>Edited</th><th>Lent To</th><th>Notes</th></tr>
        </thead>

        <tbody>
            @foreach (AndrewHomework3.Models.MovieModel x in Model)
            {
                <tr>
                    <td>@x.Category</td>
                    <td>@x.Title</td>
                    <td>@x.Year</td>
                    <td>@x.Rating</td>
                    <td>@x.Edited</td>
                    <td>@x.LentTo</td>
                    <td>@x.Notes</td>
                </tr>
            }
        </tbody>
    </table>
</div>*@
